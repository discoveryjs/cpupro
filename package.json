{
  "name": "cpupro",
  "version": "0.0.0",
  "description": "Rethinking of CPU profile analysis",
  "author": "Roman Dvornov <rdvornov@gmail.com> (https://github.com/lahmatiy)",
  "license": "MIT",
  "keywords": [
    "v8",
    "cpu",
    "profile",
    "cpuprofile",
    "ui",
    "viewer",
    "report",
    "analysis",
    "discovery"
  ],
  "scripts": {
    "lint": "eslint src",
    "start": "discovery",
    "build": "discovery-build",
    "build-app": "discovery-build --config app.config.js --clean --single-file --no-data --model-data-upload",
    "build-report-template": "discovery-build --clean --single-file --no-data",
    "build-gh-pages": "npm run build-app -- -o .gh-pages"
  },
  "dependencies": {
    "@discoveryjs/discovery": "^1.0.0-beta.63",
    "@discoveryjs/json-ext": "^0.5.6",
    "open": "^8.4.0",
    "v8-profiler-next": "^1.5.1"
  },
  "devDependencies": {
    "@discoveryjs/cli": "^2.0.0-beta.13",
    "eslint": "^8.7.0"
  }
}
